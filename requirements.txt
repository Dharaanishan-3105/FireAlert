# Streamlit Cloud / server-friendly (no GUI, no desktop audio)
opencv-python-headless>=4.8.0
numpy>=1.23.0,<2.0.0
ultralytics>=8.0.0
pillow>=9.0.0
python-dotenv>=1.0.0
streamlit>=1.28.0

# Optional for local run with alarm sound: pip install playsound
