# Cloud deploy: use opencv-python-headless (no GUI on server).
# All features unchanged: email alerts, fast fire detection, sound alert (browser plays alarm in Streamlit app).
opencv-python-headless>=4.8.0
numpy>=1.24.0
ultralytics>=8.0.0
pillow>=9.0.0
python-dotenv>=1.0.0
streamlit>=1.28.0
